export type Players = {
  X: number[];
  O: number[];
};

export type PlayersScore = {
  X: number;
  O: number;
};

export type PlayerNames = {
  X: string;
  O: string;
};

export type sign = 'X' | 'O' | '';
